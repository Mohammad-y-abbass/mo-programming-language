function test (x) {
    var a = x;
console.log(a);
}
test("This is a test")
if (1 == 1) {
    console.log("1 is equal to 1");
}
function sqrt(x) {
  return Math.sqrt(x);
}

function pow(x, y) {
  return Math.pow(x, y);
}

function floor(x) {
  return Math.floor(x);
}

function ceil(x) {
  return Math.ceil(x);
}

function displayArrElements(arr) {
  for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
  }
}
